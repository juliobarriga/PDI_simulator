El programa corre en Python 3.7
con las librerias:
-matplotlib
-tkinter
-numpy
-math
-time

por lo general solo es necesario instalar matplotlib, ya que muchas veces las otras se incluyen con el paquete de instalación de python 3.7+

Si el programa se detiene cuando se ingresa un valor en m(k) o r(k),
hay que presionar nuevamente el boton de "Iniciar", 
solo sera necesario la primera vez que se ingrese una variable en esos campos.

